# === TraceOS Iron Monolith + v2.6 Hardening ===
# Merged dependencies from tracememory + orchestrator
# Added AI/ML dependencies for real embeddings

# === Core Framework ===
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# === HTTP Clients ===
httpx==0.25.1
requests==2.31.0

# === Database & Storage ===
# SQLite is built-in, no extra deps needed
portalocker==2.8.2  # v2.6 Task 1: Multi-process migration locking

# === AI/ML - Intelligence Layer (IRON MONOLITH) ===
sentence-transformers>=2.2.0  # Real semantic embeddings
torch>=2.0.0  # Required by sentence-transformers
numpy==1.26.0
scipy==1.10.0

# === Data Processing ===
orjson==3.9.10
lxml==4.9.3
python-multipart==0.0.6

# === Utilities ===
python-ulid==1.1.0
python-dotenv>=1.0.0

# === API Integration (v2.6) ===
anthropic==0.7.1  # Claude API for compression
google-generativeai>=0.3.0  # Gemini API for critic (optional)

# === Development ===
pytest>=7.4.0
pytest-asyncio>=0.21.0

# === Optional: PyArrow for Parquet (v2.6 Task 3) ===
# Required by telemetry_store and dual_dna
pyarrow>=14.0.0
